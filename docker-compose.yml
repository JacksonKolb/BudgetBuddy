version: '3.8'
services:
  budgetbuddy-backend:
    build: ./backend
    ports:
      - "8080:8000"
    command: uvicorn main:app --host 0.0.0.0 --reload

  budgetbuddy-frontend:
    build: ./frontend
    ports:
      - "80:80"